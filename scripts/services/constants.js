const NODE = {
    div: htmlNode.div(),
    container: htmlNode.container(),
    rowDiv: htmlNode.row(),
    ol: htmlNode.ol(),
    ul: htmlNode.ul(),
    li: htmlNode.li(),
    placeHolder: htmlNode.placeHolderDiv()
}

